# CardDown CLI

The `carddown` command converts Markdown files into paginated PNG cards or PDF output.

```bash
npx carddown -i document.md
```

See the [CardDown repository](https://github.com/WiseZenn/carddown) for full documentation.
